PORT=4000
DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/mutaz_os
CLIENT_ORIGIN=http://localhost:5173

# Optional: only set this when deploying somewhere public (e.g. Vercel).
# Locally, leave it empty — no password gate, works like before.
APP_PASSWORD=

DATABASE_URL=
